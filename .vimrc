set timeoutlen=1000
set ttimeoutlen=5

" remap `leave insert mode` to jj
inoremap jj <ESC>

" highlight syntax
syntax on 

" highlight all search results
set hlsearch

" enable autoindent
set autoindent

" indenting is 2 spaces 
set shiftwidth=2

